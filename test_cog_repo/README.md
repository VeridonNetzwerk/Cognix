# Test Cog for CogniX Marketplace

A simple test cog to verify the marketplace installation flow.

## Structure
```
cogs/
  hello.py    # The actual cog with /hello and /ping slash commands
```

## Expected behavior
- `/hello` — Says hello to the user
- `/ping` — Shows bot latency
