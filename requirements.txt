# =====================================================
# CogniX — Python dependencies
# Install: pip install -r requirements.txt
#
# Optional extras are commented out; uncomment to enable.
# =====================================================

# ---- Web framework & ASGI server ----
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
starlette>=0.37.0               # pulled by FastAPI, pinned for middleware compat
jinja2>=3.1.4                   # server-rendered dashboard templates
python-multipart>=0.0.9         # required by FastAPI Form() handlers

# ---- Data validation & settings ----
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.2.0          # enables EmailStr in Pydantic

# ---- Database (ORM + migrations) ----
sqlalchemy>=2.0.30
alembic>=1.13.0
aiosqlite>=0.20.0               # SQLite async driver (default / dev)

# ---- Database drivers (uncomment as needed) ----
# asyncpg>=0.29.0               # PostgreSQL
aiomysql>=0.2.0                 # MySQL / MariaDB

# ---- Redis (optional; only needed for IPC/event streaming) ----
# redis>=5.0.0

# ---- Discord bot ----
discord.py>=2.4.0
psutil>=6.0.0                   # memory/CPU stats in bot status

# ---- Security & auth ----
cryptography>=42.0.0            # AES-256-GCM encryption
PyJWT>=2.8.0                    # JWT access / refresh tokens
bcrypt>=4.1.0                   # password hashing
pyotp>=2.9.0                    # TOTP 2FA
qrcode[pil]>=7.4.2              # QR codes for TOTP enrollment

# ---- Google OAuth (optional — remove if not using) ----
authlib>=1.3.0
httpx>=0.27.0                   # HTTP client used by Authlib + test utilities

# ---- Logging ----
structlog>=24.2.0

# ---- Music cog (native voice via discord.py + yt-dlp + FFmpeg) ----
# FFmpeg must also be installed on the host (system package).
yt-dlp>=2024.8.6
PyNaCl>=1.5.0                   # required by discord.py voice
Pillow>=10.0.0                  # avatar/banner image handling
